spring:
  application:
    name: rmi-calculator

  datasource:
    url: jdbc:postgresql://postgres.railway.internal:5432/railway?sslmode=require
    username: postgres
    password: yDyvAZkFjDvirsfOxxAKaMEMXzYsLipZ

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
  servlet:
    context-path: /

rmi:
  registry:
    port: 1099
  service:
    name: CalculatorService
